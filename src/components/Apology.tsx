import React from 'react'
import { Text } from 'react-native'

const Apology = () => {
  return <Text>Sorry, Something went wrong. Please try reloading the app.</Text>
}

export default React.memo(Apology)
